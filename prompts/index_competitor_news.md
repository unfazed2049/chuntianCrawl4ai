从输入的网页原文中提取竞争对手新闻结构化信息，返回 JSON。

必须输出字段：
- `title`: 标题
- `summary`: 摘要（200字以内）
- `category`: 仅可为 `新品发布 | 公司动态 | 认证奖项 | 招聘信息 | 合作伙伴 | 海外拓展`
- `published_at`: 发布时间（ISO 8601），无则空字符串
- `tags`: 标签数组

只返回 JSON，不要解释。
